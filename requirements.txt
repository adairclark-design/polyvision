# PolyVision — Python Dependencies
# Install: pip install -r requirements.txt

# ── Data & APIs ───────────────────────────────────────────────────────────────
requests>=2.31.0
python-dotenv>=1.0.0
boto3>=1.34.0

# ── Polymarket SDK ────────────────────────────────────────────────────────────
py-clob-client>=0.16.0

# ── AI ─────────────────────────────────────────────────────────────────────────
openai>=1.30.0
anthropic>=0.28.0        # agent_reflector.py — Claude Sonnet nightly reflection

# ── Video & Asset Generation (Marketing Pipeline) ─────────────────────────────
creatomate>=1.4.0         # video_factory.py — Creatomate video render API
fal-client>=0.5.0         # background_generator.py — Kling AI video backgrounds

# ── Databases ─────────────────────────────────────────────────────────────────
psycopg2-binary>=2.9.9
redis>=5.0.0

# ── Notifications ─────────────────────────────────────────────────────────────
# OneSignal, Discord, and Telegram use plain HTTP via requests (no extra lib needed)

# ── Reporting (Phase 4 — Alpha PDF) ──────────────────────────────────────────
reportlab>=4.2.0

# ── X / Twitter Bot Engine ───────────────────────────────────────────────────
tweepy>=4.14.0          # Twitter API v2 client (broadcaster, monitor, reply agent)
feedparser>=6.0.11      # RSS ingestion for the News-Jacking engine

# ── Image Generation (Card Generator) ────────────────────────────────────────
Pillow>=10.3.0          # Headless PNG card rendering — no display required
